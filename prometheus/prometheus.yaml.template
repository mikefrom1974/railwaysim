global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'train-telemetry-sink'
    static_configs:
      - targets: ['${EXPORTER_TARGET}'] # swapped at runtime via entrypoint